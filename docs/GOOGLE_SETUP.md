# Google setup

Search Console is justified for the URL migration. Verify the existing property, export historical pages/queries, submit `https://viste.ai/sitemap.xml` after launch and monitor coverage/404 changes.

GA4 is optional and consent-gated. Add only the measurement ID to Vercel after approval. No Google Maps, reCAPTCHA, runtime Google Fonts or Google mailbox access is required. OpenAI is also unnecessary for launch and stays disabled.
