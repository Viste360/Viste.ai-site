"use client";import {useState} from "react";import {createClient} from "@supabase/supabase-js";
type Lead={id:string;created_at:string;name:string;email:string;company:string;country:string|null;budget:string;timeline:string;status:string;challenge:string};
export function Admin(){const [email,setEmail]=useState("");const [message,setMessage]=useState("");const [leads,setLeads]=useState<Lead[]>([]);const url=process.env.NEXT_PUBLIC_SUPABASE_URL;const key=process.env.NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY;async function login(e:React.FormEvent){e.preventDefault();if(!url||!key){setMessage("Supabase is not configured.");return}const supabase=createClient(url,key);const {error}=await supabase.auth.signInWithOtp({email,options:{emailRedirectTo:`${location.origin}/admin`}});setMessage(error?error.message:"Check your email for the secure sign-in link.")}async function load(){if(!url||!key)return;const supabase=createClient(url,key);const {data}=await supabase.auth.getSession();const response=await fetch("/api/admin/leads",{headers:{authorization:`Bearer ${data.session?.access_token}`}});if(response.ok)setLeads((await response.json()).leads);else setMessage("This account is not authorised.")}return <main className="admin shell"><p className="eyebrow">Secure workspace</p><h1>Lead inbox</h1><form onSubmit={login} className="admin-login"><label>Authorised email<input value={email} onChange={e=>setEmail(e.target.value)} type="email" required/></label><button className="button">Send sign-in link</button><button type="button" className="button button-ghost" onClick={load}>Load leads after sign-in</button></form><p role="status">{message}</p><div className="admin-leads">{leads.map(x=><article key={x.id}><span>{new Date(x.created_at).toLocaleString()} · {x.status}</span><h2>{x.company} — {x.name}</h2><a href={`mailto:${x.email}`}>{x.email}</a><p>{x.challenge}</p><small>{x.country} · {x.budget} · {x.timeline}</small></article>)}</div></main>}
