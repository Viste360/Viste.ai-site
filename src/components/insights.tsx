import Link from "next/link";
import type { Insight } from "@/content/types";
import { insights } from "@/content/insights";

export function InsightsIndex({ locale }: { locale: "en" | "es" }) { return <main><section className="page-hero"><div className="shell narrow"><p className="eyebrow">{locale === "en" ? "Insights" : "Recursos"}</p><h1>{locale === "en" ? "Practical guidance for responsible AI implementation" : "Orientación práctica para implementar IA responsable"}</h1><p className="lede">{locale === "en" ? "Clear thinking on use cases, operating models, human oversight and the path from pilot to production." : "Ideas claras sobre casos de uso, modelos operativos, supervisión y el paso de piloto a producción."}</p></div></section><section className="shell insight-list">{insights.map((x)=><Link href={x.path[locale]} key={x.id}><div><span>{x.publishedAt} · {x.readTime[locale]}</span><h2>{x.title[locale]}</h2><p>{x.description[locale]}</p></div><b>↗</b></Link>)}</section></main>; }

export function InsightArticle({ insight, locale }: { insight: Insight; locale: "en" | "es" }) { return <main><article><header className="page-hero article-hero"><div className="shell narrow"><Link className="text-link" href={locale === "en" ? "/insights" : "/es/recursos"}>← {locale === "en" ? "Insights" : "Recursos"}</Link><p className="eyebrow">{insight.publishedAt} · {insight.readTime[locale]}</p><h1>{insight.title[locale]}</h1><p className="lede">{insight.description[locale]}</p></div></header><div className="shell article-body">{insight.sections[locale].map((s)=><section key={s.title}><h2>{s.title}</h2>{s.paragraphs.map((p)=><p key={p}>{p}</p>)}{s.bullets?<ul>{s.bullets.map(b=><li key={b}>{b}</li>)}</ul>:null}</section>)}</div></article></main>; }
