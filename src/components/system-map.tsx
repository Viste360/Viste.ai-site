export function SystemMap({ locale }: { locale: "en" | "es" }) {
  const copy = locale === "en" ? { eyebrow: "A system, not a gimmick", title: "People, process, data and AI—designed together", center: "Controlled AI layer", cards: ["Business systems", "Approved knowledge", "Human decisions", "Metrics & monitoring"], foot: "Every connection is scoped, permissioned and measured." } : { eyebrow: "Un sistema, no un truco", title: "Personas, procesos, datos e IA diseñados juntos", center: "Capa de IA controlada", cards: ["Sistemas de negocio", "Conocimiento aprobado", "Decisiones humanas", "Métricas y supervisión"], foot: "Cada conexión se define, autoriza y mide." };
  return <section className="system-section"><div className="shell split"><div><p className="eyebrow">{copy.eyebrow}</p><h2>{copy.title}</h2><p>{copy.foot}</p></div><div className="system-map" aria-label={copy.title}><div className="system-core"><span className="core-pulse" aria-hidden="true" /><strong>{copy.center}</strong></div>{copy.cards.map((card, index) => <div className={`system-node node-${index + 1}`} key={card}><span className={`node-signal node-signal-${index + 1}`} aria-hidden="true" /><strong>{card}</strong></div>)}</div></div></section>;
}
