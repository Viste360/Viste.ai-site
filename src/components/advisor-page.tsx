export function AdvisorPage({ locale }: { locale: "en" | "es" }) {
  const es = locale === "es";
  return <main className="advisor-page"><section className="page-hero advisor-hero"><div className="shell narrow"><p className="eyebrow">VIS_010 · Viste Opportunity Engine</p><h1>{es ? "Empieza por el problema, no por un formulario." : "Start with the issue, not a form."}</h1><p className="lede">{es ? "El Asesor de Viste está abierto en la esquina. Dos preguntas breves bastan para proponer un punto de partida; los datos de contacto solo aparecen si quieres una revisión humana." : "The Viste Advisor is open in the corner. Two short prompts are enough to suggest a starting point; contact details appear only if you request human review."}</p><div className="advisor-page-points"><span>{es ? "2 preguntas" : "2 prompts"}</span><span>{es ? "Recomendación inmediata" : "Immediate recommendation"}</span><span>{es ? "Revisión humana opcional" : "Optional human review"}</span></div></div></section></main>;
}
